---
- name: Play to setup infrastructure on OpenStack 
  gather_facts: false
  collections:
    - gpte.openstack

  roles:
    - role: create-instances
      vars:
        guid: "{{ lookup('ENV','GUID') }}"
